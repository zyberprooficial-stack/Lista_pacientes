// Configuración del Frontend
// IMPORTANTE: Editar este archivo después del deploy para apuntar al backend en Render

window.ENV = {
    // Cambiar esta URL después de deployar el backend en Render
    // Ejemplo: 'https://pacientes-backend-xxxx.onrender.com/api'
    API_BASE_URL: 'http://localhost:8080/api',
    
    // Otras configuraciones
    ENABLE_DEBUG: false
};
